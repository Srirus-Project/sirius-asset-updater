# JSON is valid YAML. Replace the CDN placeholder with the selected server-list base URL.
{
  "region": "en",
  "platform": "Android",
  "protocol_version": "1.0.1",
  "game_api_root": "http://127.0.0.1:9999",
  "internal_token_env": "SIRIUS_EN_INTERNAL_TOKEN",
  "refresh_token_env": "SIRIUS_EN_API_TOKEN",
  "environment": "release",
  "client_version": "1.0.1",
  "output": "./downloads/en/catalogs",
  "cdn_roots": {
    "https://cdn.example.invalid/prod/en_REPLACE_FROM_SERVER_LIST": {
      "username_env": "SIRIUS_EN_CDN_USERNAME",
      "credential_env": "SIRIUS_EN_CDN_CREDENTIAL"
    }
  }
}
