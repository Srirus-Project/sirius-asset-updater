# Sirius 1.2.0 restoration and acceptance ledger

Objective: retain Haruki's reusable service/platform capabilities, replace all Sekai-specific logic with Sirius implementations, test the complete production pipeline on yhm01, then publish both repositories as 1.2.0. A working CLI or one successful export alone is not completion.

Reference baseline: local Haruki-Sekai-API 07da6b80e6a59ece89251f4694afe94bea72e131 and Haruki-Sekai-Asset-Updater 3d33ed037f0ef5009e361e0535b3b19f8c239947. Preserve MIT attribution. Existing Sirius release baselines: API 5ab1e390ca514e550cb609ae4d299b19c105b79e, updater e7ffb7b905958de88aa8d5a06327dafcc8acd8ce.

## API requirements (all pending)

- Multi-region service configuration/routing with per-region protocol family, credentials and isolated state; retain v1.1 single-region config compatibility and reserved CN.
- Account pool, per-account locking, selection, health/cooldown and credential reload. No speculative retries of account mutations; no fabricated Global login.
- Configurable response cache, optional Redis backend, region/account/protocol-aware identity and invalidation; private data never shared through public cache.
- Upstream proxy, deadlines/limits, controlled retry policy, server TLS, logs/access logs and trusted-forwarding configuration.
- Remote node routing, priorities/failover, token-scoped internal calls and capability/version boundaries.
- Master registry/manifests, owner/consumer synchronization, notifications, optional generic persistence and Git publication. Do not restore Sekai table models or Ent code.
- Full documented config surface, examples, migration, meaningful local integration tests including failure/authorization cases.

## Updater requirements (all pending unless specifically evidenced)

- Authenticated long-running HTTP service, region configs, job submission/list/detail/cancel/retry, bounded queue/concurrency, progress, retention and safe shutdown/restart.
- Invoke Sirius catalog/download/verify/export pipeline from the service, not a stub or arbitrary shell executor; retain CLI support.
- Incremental download/export records by verified content identity; cache reuse, partial failure recovery and immutable publication semantics.
- Include/exclude filters, priority ordering, exact type selection and scoped replay; selected work is never claimed as full-catalog verification.
- OpenDAL/local/S3 providers, upload concurrency/retries and verified upload-before-cleanup policy; region-scoped object keys and secret references.
- Format configuration for image/audio/video and backend choice where implementable for Sirius; retain correct alpha/split audio semantics.
- Separate stage concurrency, bounded resource budgets, configurable retries/timeouts, logging/access logging and TLS/proxy configuration.
- Update trigger/scheduling, completion notification and generic publication integration where applicable. Sekai chart-hash/character-ID/3D model adapters are removed, not copied.
- End-to-end tests cover queue saturation, cancellation, restart, per-region serialization, incremental behavior, upload failure safety and secrets.

## Release gates (all pending)

- Audit each feature against original code/config; no placeholders or silent ignored config fields; record any game-specific non-applicability with evidence.
- Both repositories fmt/check/Clippy/tests and release packaging smoke pass.
- yhm01 official candidate full JP download/decrypt/export through restored service; retain and independently hash every exported file.
- yhm01 verify incremental second run, job API/auth/cancel, resource limits, restart and configured storage path; existing Haruki services unchanged.
- Global remains limited to verified contracts unless additional protocol research validates functionality; region isolation test for TW/EN/KR and CN rejection.
- Public source/artifact secret audit, three-platform artifacts, exact commits, version 1.2.0 tags/releases, published artifact audit.

## Work log

- Initial audit confirmed the missing components are real scope loss, not merely shorter examples/defaults.
- Implementation begins with the updater's durable job lifecycle and service orchestration; API restoration follows against the same ledger.

- Updater durable job ledger implemented and five lifecycle tests passed: restart interruption, exclusive state ownership, queue/retention bounds, per-region scheduling, cancellation acknowledgement, storage failure atomicity and reserved-region rejection. Authenticated HTTP and real pipeline integration were added in 7efe562; a local HTTP verification/queue/auth/retry test passes. Running-work cancellation, timeout/restart and full production service acceptance remain pending.

- User requires category investigation before adapting filters. Verified Sirius catalog labels: InitialDownload, Everything, MV; use native key/dependency membership, not Sekai start_app/on_demand. Everything is a subset of full catalog (JP baseline: 13,364 versus 13,367 remote files). Exact tutorial/runtime required-address composition remains unverified; no invented preset.

- Native catalog string keys and transitive dependencies are now implemented, with root include/exclude regex and ordered remote-path priorities. Selection is persisted in receipts and verified offline; partial work explicitly reports full_catalog=false. Native labels are tested against the investigated JP catalog, including legacy graph byte compatibility. Exact export type selection and remaining filter/service acceptance gates remain pending.

- API multi-region assembly restored; updater now supports opt-in `regional_routes` for version refresh and snapshots, preserving legacy routes and token scopes. A local complete catalog publication test exercises both regional HTTP endpoints and CDN credential separation.

- Export type selection now uses exact Unity class IDs and native provider groups, with explicit embedded-audio control and schema-3 selected/skipped counts. Added native PNG/WebP/BMP/TGA/JPEG output (JPEG requires a compositing background) and FLAC output verified against exact decoded PCM. Independent FFmpeg roundtrips verify lossless RGBA, row orientation and FLAC; video format/backend choice and remaining export/publication acceptance are still pending.

- Service lifecycle tests now exercise actual asynchronous downloads against a blocked local upstream: running cancellation, per-region serialization, shutdown drain/admission stop, preserved queues after reopen, interrupted-worker recovery and retry after a distinct job_timeout. Final verification/export counters are persisted without waiting for the periodic poll. Unix media-child tests prove cancellation/deadline paths kill and reap the child. Full export-through-service cancellation/storage-failure injection and yhm01 acceptance remain pending.

- Download request policy now exposes validated connection/catalog/asset/snapshot/refresh timeouts, separate snapshot/catalog/asset attempts and bounded exponential backoff, and between-batch revalidation intervals. Existing defaults and credential scopes are preserved. Local HTTP tests count actual retry attempts, check terminal 403 behavior, enforce body timeouts and cancel long backoffs without publication. Full fmt/check/Clippy/test validation passes (62 passed, 5 optional tests ignored); proxy/TLS/logging and other resource controls remain pending.

- Incremental decoded export is now opt-in with private content-addressed resource entries, per-cache process ownership, source/dependency/config/region/executable identity, checksummed reports and size/SHA-256 verification of every copied output. Corrupt entries are redecoded; successful entries survive partial failures/cancellation/restart, and copies cannot modify earlier publications. Schema 4 records cache hits. Synthetic HCA export verifies exact second-run bytes, corruption repair, failed-resource exclusion, cancellation and invalidation. Local authenticated HTTP service export and restart/reuse pass with real FFmpeg present. fmt/check/Clippy and the default suite pass (66 passed, 6 optional tests ignored); the optional service export/restart test was also run explicitly and passed. Automatic eviction, upload/publication backends and full production incremental acceptance remain pending.

- Optional inbound TLS is now wired into the production listener, with bounded PEM reads, certificate/key matching, Unix private-key permissions, ALPN HTTP/1.1+HTTP/2 and a configurable handshake timeout. Certificates load before bind/background workers. Existing plaintext deployment remains supported; TLS closes plaintext/stalled peers and gracefully drains active requests. API multi-region TLS belongs only at the deployment root. fmt/check/Clippy and the default suite pass (69 passed, six optional media/catalog tests ignored). Structured/access logging and forwarding trust remain pending.

- Access logging is now configurable at the listener root: JSON/text, stdout or rotating files, bounded asynchronous queue and retention. Records contain only matched route templates, generated request IDs, peer/client IPs, outcome/status and header-response latency; credentials, query strings, bodies and route IDs are excluded. Trusted forwarding walks explicit CIDRs from the socket peer toward the first untrusted hop and rejects malformed/duplicate/oversized headers. Tests exercise redaction, spoofed chains, IPv6, cancellation, append behavior and HTTPS/HTTP2 listener integration. fmt/check/Clippy pass; full default suite: 72 passed, six optional tests ignored. An initial concurrent validation run hit the pre-existing media-fixture startup time bound; its isolated rerun and a full standalone rerun passed without changing that test. Application log configuration remains pending.

- Storage publication preparation now independently verifies retained schema-4 exports: bounded journal reads, exact resource/file inventory, size/SHA-256 checks, receipt/counter consistency, region identity and rejection of links/unlisted paths. `verify-export DIRECTORY REGION` provides the same offline check; retained service exports run it before completion and persist export-verification.json. A temporary JSON-lines allowlist includes payloads and both receipts for future upload. Scope flags remain receipt declarations, not independent source-catalog proof. fmt/check/Clippy and the default suite pass (76 passed, six optional tests ignored); the real FFmpeg HTTP service export/restart/cache-reuse integration was run explicitly and passes with verification enabled. OpenDAL/local/S3 upload, remote read-back, retry/cleanup policies and production storage acceptance remain pending.

- OpenDAL 0.58.2 local/S3 publication now runs through CLI `publish` and service profile `storage_config`. Region/UUID prefixes preserve earlier publications; all required destinations receive streamed objects, read-back size/SHA-256 checks and final verified completion markers before opt-in source cleanup. Explicit environment credentials, no implicit AWS credentials/proxies/redirects, bounded object concurrency/timeouts/retries and multipart cancellation/abort are implemented. Local/S3 mock tests cover success, transient retry, write/read permission failure, corruption, redirect rejection, marker failure, multi-destination failure, empty objects, timeout and multipart cancellation. A real FFmpeg HTTP service test exercises export, verified publication, restart/cache reuse and storage failure with retained source outputs. fmt/check/Clippy/default tests pass (82 passed, six optional tests ignored); the FFmpeg service test passes explicitly. A temporary macOS archive using the current debug binary passes updated packaging smoke including publish; this is not a release candidate or three-platform acceptance. Apache license/notice and storage examples are packaged. Remaining storage audit includes other generic OpenDAL backends, publication registries/notifications and yhm01 production verification.

- Download clients now support independent `network.api_proxy` and `network.cdn_proxy` HTTP/HTTPS forward proxies through explicit URL/authorization environment references. Version refresh/snapshot and catalog/asset clients are isolated, preserve origin credential scopes and disable redirects and implicit system/environment proxy discovery. Offline readiness reports missing references or invalid fields without values. Tests execute refresh, snapshot, catalog and asset requests through distinct proxies, verify CONNECT keeps proxy auth outside origin TLS, enforce origin/proxy certificate validation and timeouts, reject redirects/407 without direct fallback, and confirm ambient proxy variables are ignored in a separate process. fmt/check/Clippy/default tests pass (88 passed, six optional tests ignored). Storage uses its separate transport; application logging, other restoration groups and production acceptance remain pending.

- Application logging is now configured at the executable/service root: off/error/warn/info/debug/trace, text/JSON, stderr/stdout or UTC-rotating append files, bounded queue and retention. It reuses the access-log sink but filters to application targets and an explicit operational-field allowlist; dependency request payloads and raw resource/decoder details are excluded. Updater stages/retries/progress and persisted job terminal states emit structured events. Default events use stderr, preserving CLI stdout; existing CLI diagnostics remain separate. Multi-region/per-profile nested logging is rejected. Tests verify levels, forbidden fields/dependency targets, numeric JSON fields, escaped newlines, UTF-8 limits, append/flush, disabled logging and invalid sinks. fmt/check/Clippy/default tests pass (90 passed, six optional media/catalog tests ignored). Both actual local service processes also passed JSON file output, clean stdout and SIGTERM exit/flush checks; the updater check included a persisted failed job and its terminal log event. Other restoration groups and full yhm01/three-platform release acceptance remain pending.

- Audited original automatic asset triggers: API cron/version ownership dispatches HTTP jobs; the asset service has no separate recurring timer or WebSocket route. Documented responsibility split and pending owner reconciliation in docs/UPDATE_TRIGGERS.md. Added optional durable Idempotency-Key submission: same request returns the retained job across concurrent retries, queue saturation and restart; changed requests conflict; invalid/duplicate headers fail; only a SHA-256 digest is persisted. Retention bounds deduplication and keys do not pin catalog versions. Explicit retry remains new work. Tests cover 12 concurrent HTTP retries, authorization, malformed headers, restart, retention, persistence failure and duplicate ledger identities. fmt/check/Clippy/default suite pass (92 passed, six optional tests ignored). API dispatch state, receipt reconciliation, notifications and full production/release gates remain pending.

- Successful service jobs now persist a structured outcome atomically with completed status: verified catalog digest and receipt identity, full-catalog scope/counters, optional export scope/retention/counts, and optional verified storage publication UUID. Export digest/region/platform/full-catalog scope must match input verification. Failed/cancelled/interrupted jobs and retries do not inherit success outcomes; older completed ledger records remain readable without them. Tests exercise completion-write failure, restart, region mismatch, cancelled/failed/retry states, HTTP receipt persistence, and real FFmpeg export/cache-restart/storage-success/failure results. fmt/check/Clippy/default suite pass (93 passed, six optional tests ignored); the FFmpeg service integration was run explicitly and passed. This exposes the reconciliation contract but does not complete API-owned dispatch or yhm01/release acceptance.

- Audited Haruki video options (M2V/MP4 and direct_mp4) against Sirius USM M2V/IVF and independent alpha tracks. Added real source/mkv/mp4/mkv_and_mp4 selection with backward-compatible MKV default. Original elementary streams and selected PCM/FLAC outputs remain retained; MP4 uses explicit H.264/AAC compatibility encoding and preserves separate alpha semantics. Every requested container is decoded and checked against declared frame count; summaries and cache identity include video mode. A real FFmpeg synthetic video+audio test covers all mode combinations, independent raw-frame/audio decoding, source preservation and wrong-frame-count rejection; cache tests verify mode invalidation. fmt/check/Clippy/default suite pass (93 passed, seven optional tests ignored); the new FFmpeg test was explicitly run and passed. Backend choice, stage budgets and remaining production/release gates remain pending.

- Restored separate per-export media admission with media_concurrency (1..4, default 2) independent of resource concurrency. All FFmpeg processing children share a cancellable gate; media_timeout_seconds includes slot wait and execution. Permit RAII releases on completion, cancellation/deadline and spawn failure. Actual subprocess tests launch eight contenders at caps one/two, verify bounded overlap, cancel queued work before spawn, enforce queued timeout and recover after missing-executable failure. fmt/check/Clippy/default suite pass (94 passed, seven optional tests ignored); the real FFmpeg video-format regression also passes explicitly. Limits are per job, not a host-wide memory cap. Other stage/resource controls, backend audit, production acceptance and release gates remain pending.

- Restored MP3 compatibility audio alongside the existing WAV/FLAC modes. MP3 output preserves the original PCM WAV and keeps video muxing on PCM, uses libmp3lame with sample-rate-appropriate bitrates, and decodes the result to verify channels/rate and bounded frame-padding difference. Supports all nine native MP3 sample rates in mono/stereo; unsupported rates/multichannel are explicit errors rather than implicit resampling/downmixing. Both outputs enter budgets/journals/publication and cache mode identity changes. Actual FFmpeg tests cover 18 rate/channel combinations, nonempty independent raw decode, source byte preservation, unsupported shapes and encrypted synthetic ACB. fmt/check/Clippy/default suite pass (94 passed, eight optional tests ignored); MP3 integration was explicitly run and passed. Simultaneous arbitrary format lists, backend choices and other restoration/production/release gates remain pending.

- Restored simultaneous audio output lists while preserving scalar wav/flac/mp3 compatibility. Canonical nonempty selections reject duplicates/unknowns and normalize order/singletons for cache and receipt identity. All seven combinations work; explicit WAV remains, MP3 without FLAC retains WAV, and FLAC+MP3 removes temporary WAV only after successful conversions and exact FLAC PCM roundtrip. No duplicate journal records or lossy intermediate for video muxing. Tests verify configuration normalization plus every output combination, source preservation, independent FLAC decode and MP3 regression. fmt/check/Clippy/default suite pass (95 passed, nine optional tests ignored); real FFmpeg combination, MP3, image/FLAC fidelity and export/cache/restart/storage service tests were explicitly run and passed. Backend/resource/cache/notification and other platform groups plus production/release acceptance remain pending.

- Completed a source-backed backend inventory in docs/BACKEND_RESTORATION_AUDIT.md. Verified original checkout HEAD and Cargo features: only OpenDAL FS/S3 service families are compiled, both already implemented in Sirius; no unrelated third backend is inferred from generic scheme/options. Original S3 address-style, public ACL/filter and public URL/planning options are real missing generic controls. Original media-ffi is a real rsmpeg/FFmpeg 7.x implementation with its own CI, so FFI/Auto selection remains required rather than being dismissed as game-specific. The audit resolves service-family ambiguity and identifies exact adapter/config work; no runtime changes or test reruns were needed for this documentation-only commit. Production and release gates remain pending.

- Restored explicit S3 path_style selection with true as the backward-compatible default. Virtual-host mode uses the S3 builder switch and rejects IP endpoints/dotted buckets before requests. A capturing transport verifies actual SigV4-signed request authority, port, escaped path and signing scope for both modes without contacting an external host; legacy YAML defaults and invalid combinations are covered. fmt/check/Clippy/default suite pass (96 passed, nine optional fixture/media tests ignored). Storage ACL/public URL controls, media backends and all remaining restoration/production/release gates remain pending.

- Restored S3 public_read and per-provider include/exclude regex rules with explicit exclusion precedence, including over the provider-wide flag. Policies are bounded/compiled before requests; matched ordinary writes, multipart initiation and publication markers carry public-read ACL, unmatched writes preserve the no-ACL default. No silent fallback on ACL rejection; local exports remain on failure. Authenticated read-back remains required and does not claim anonymous-access verification. Mock S3 tests cover defaults, include/exclude/all policies, multipart upload, markers, invalid rules and rejection without cleanup. fmt/check/Clippy/default suite pass (97 passed, nine optional tests ignored). Documented migration from Haruki global patterns and provider-wide precedence. Public URL/registry controls, FFI media and remaining production/release gates remain pending.

- Restored provider public_base_url and plan-storage CLI. Preview validates configuration/secrets without reading exports, creating storage directories or contacting providers; marks its UUID as illustrative. Actual publications share target construction and persist optional HTTPS directory URLs with region/UUID prefixes. Credentials, query/fragment URLs are rejected; backend origin/local roots are not emitted. Tests cover JP/TW/EN/KR isolation, CN refusal, path-prefix preservation, omitted URL compatibility, invalid URLs and actual local publication targets. Actual CLI smoke passed with absent input and no destination creation. fmt/check/Clippy/default suite pass (98 passed, nine optional tests ignored), and cargo build passes. CDN availability, registry/notification integration, remaining restoration and full production/release gates are still pending.

- Restored per-object verified upload progress through a bounded watch snapshot and service persistence at one-second intervals. Counts aggregate all destination copies, advance only after read-back validation, and identify upload provider index, marker and optional cleanup phases. Final service counts use destination totals while publication receipts remain per-destination. A progress persistence error cancels and drains publication. Mock storage tests pause after one verified object, check partial/aggregate progress across two destinations and ensure corrupted read-back is not counted. fmt/check/Clippy/default suite pass (99 passed, nine optional tests ignored); real FFmpeg service export/cache-restart/storage integration also passes explicitly. Production storage/resource acceptance, media backends, other restoration groups and release gates remain pending.

- Restored decoded-cache capacity controls: optional positive cache_max_bytes and 1..1000000 cache_max_entries, requiring a configured cache. Owned startup scans managed entries, removes interrupted staging, and enforces LRU budgets using durable entry modification times; verified hits update recency. Inserts evict before staging, over-budget individual entries bypass caching without failing their export, and shared in-process locking protects copies from eviction. Tests cover LRU, inventory reconstruction, byte/count limits, 12 concurrent inserts, preserved output copies and symlink boundaries. Documented logical-size accounting, one-entry replacement staging overhead, unknown-name exclusion and serialized cache copies. Initial full regression exposed a transient Busy on successive download-cache use; explicit Guard unlock plus a duplicate-file-description regression fixes the delayed-release boundary. fmt/check/Clippy and full default suite pass (102 passed, nine optional tests ignored), with the subsequently added lock test also passing separately; real FFmpeg service/cache-restart/storage integration passes. Other resource/backend/platform restoration and full yhm01/release acceptance remain pending.

- Ported the original generic FFmpeg codec/RAII/memory-IO layer behind optional media-ffi using rsmpeg 0.18 with FFmpeg 7.1/system linking, without Sekai logic or redundant HCA decoding. Actual FFmpeg 7 builds and synthetic conversions pass; independent FFmpeg decoding covers FLAC exact PCM and MP3 channel/rate/padding across nine native rates and mono/stereo, plus unsupported shape rejection. Fixed custom AVIO buffer ownership, early allocation cleanup and channel-layout validation; runtime checks require FFmpeg 7 ABI majors. Added separate FFmpeg 7 CI job (not yet remotely run) and preserved feature-disabled checks/attribution. fmt/default check/all-feature Clippy pass; default suite 103 passed/nine ignored, FFI suite 113 passed/ten ignored; independent audio test explicitly passes. This is a real tested codec foundation, NOT completed FFI/Auto export support: pipeline selection, cancellation/deadline/admission, video equivalence, local-IO/log boundaries, cache/runtime identity and packaging are still required in docs/MEDIA_FFI.md. Other restoration groups and yhm01/full release acceptance remain pending.

- Added controlled FFI calls with shared cancellation/absolute deadlines, distinct errors, RAII scope reset and Arc-owned FFmpeg input interrupt callbacks. Checked codec operations observe cancellation; actual long synthetic conversion tests cancel after output creation, expire a running conversion, and recover on the next call. Cross-thread callback/unwind tests pass. Inputs now require absolute paths, preserve Unix bytes, and apply a file-only protocol whitelist; a local playlist test proves no HTTP connection is opened. Raw FFmpeg logging is quiet once per process. This is cooperative control, not forced interruption of one codec/OS call; callers still own staging/partial cleanup. fmt/default+feature checks/all-feature Clippy pass; default suite 103 passed/nine ignored and FFI suite 116 passed/ten ignored; independent FFI audio roundtrip explicitly passes. Export backend selection/admission wiring, video equivalence, packaging and all remaining restoration/production/release gates remain pending.

- Aligned FFI video encoding with Sirius CLI intent: explicit libx264 medium/CRF18/yuv420p/two threads, AAC192k, faststart, validated frame rates and positive even dimensions; no guessed 30fps or silent dimension changes. MP3 explicitly selects libmp3lame. Independent synthetic M2V/IVF tests, each direct and in MKV with PCM, verify 12 decoded frames, 25fps, H.264/pixel format/dimensions, AAC shape/padding, preserved sources and MP4 atom order; invalid rate/odd dimensions fail before output creation. These tests uncovered and fixed FFmpeg 7 zero packet duration hiding the final frame through MP4 edit duration. fmt/default+feature checks/all-feature Clippy pass; default suite 103 passed/nine ignored, FFI suite 116 passed/eleven ignored; both independent audio/video tests explicitly pass. Added video integration to FFmpeg CI (remote run still pending). Constant-rate synthesis is verified; variable/nonzero-start timing review, real Sirius/alpha fixtures, adapter wiring, runtime packaging and full production/release gates remain pending.

- Connected media_backend cli/ffi/auto to actual FLAC/MP3/MP4 export encoders. Default CLI and feature-disabled Auto remain usable; explicit FFI validates build/runtime availability. FFI shares media admission, cancellation and the original absolute deadline; failed partial output is removed before Auto CLI fallback, and timeout/cancellation never retries. Requested backend and encoding/fallback counters are persisted; cache identity includes policy and linked codec version/build digest. Native streams, lossless validation, frame checks and CLI mux/verification remain in place. Timeline review uncovered a 40 ms video offset in MPEG-2/PCM MKV that frame-count tests missed: nonzero muxed starts, variable video timing and audio discontinuities now fail explicitly in FFI and may fall back through Auto. Actual MPEG-2/PCM, nonzero-start and variable-rate fallback tests compare audio/video frame timestamps to independent CLI output. Full fmt/default check/all-feature Clippy/default suite pass (104 passed, eleven optional tests ignored); feature suite 117 passed/seventeen ignored. Explicit FFI audio/video/export/service-cache-restart tests (five), feature-enabled Auto tests (three), and feature-disabled Auto tests (two) pass. CI includes the new export/service/fallback tests but remote CI is not yet evidence. Real Sirius/alpha fixtures, broader FFI timing support, runtime packaging, remaining platform groups, yhm01 full acceptance and release gates remain pending.

- Restored typed S3 write policy from original scalar OpenDAL options: backend.write_options.storage_class, server_side_encryption (AES256/aws:kms), and kms_key_id_env. Parameters apply to private/public operators, ordinary PUT, multipart initialization and completion markers while preserving verified readback and cleanup gating. Unknown policy fields, malformed class/header values and incompatible KMS settings fail before upload; key identifiers stay environment-referenced. Real local S3 multipart publication verifies the emitted headers and successful readback; invalid configuration tests cover missing/unsafe keys and unsupported policy combinations. Updated storage migration/example and corrected the stale FFI audit to reflect actual CLI/FFI/Auto integration. fmt/check/all-feature Clippy/default suite pass (106 passed, 11 optional fixture/media tests ignored). Cloud KMS/bucket acceptance, remaining scalar/template/resource controls, publication notifications/registry and full yhm01 candidate/release gates remain pending.

- Added service-wide max_media_processes (default 4, range 1..16) shared by all export jobs, on top of each profile's existing media_concurrency. Both CLI children and FFI conversions acquire local then shared slots within the same absolute deadline; cancellation/failure releases acquired slots, and Auto fallback releases before reacquiring. Standalone CLI behavior remains unchanged. Independent export configurations launch actual child fixtures concurrently to verify the aggregate cap, cancellation while waiting, timeout and subsequent recovery. An existing one-second child-reaping fixture repeatedly timed out before its shell could write its PID under parallel suite load; diagnostic confirmed the timeout outcome, and only test startup allowance was increased to five seconds versus a 60-second child, retaining early-kill/reap assertions. fmt/check/all-feature Clippy/default suite pass (107 passed, 11 optional tests ignored). yhm01 continues the earlier 1b095ab candidate unchanged, so that run does not verify this new service budget. Shared download/upload/memory admission, remaining restoration and final candidate/release gates remain pending.

- Added service-wide max_uploads (default 4, range 1..32) shared across storage publications, retaining each publication's concurrency limit. Permits cover private/public local/S3 object attempts, readback and completion markers; held through bounded abort and released before backoff. Admission happens before source streams/writers, and queue wait/source metadata/writer creation/upload/readback now use one absolute per-attempt deadline rather than resetting writer and transfer budgets. Tests run two real local S3 publishers with one shared slot, verify only one request starts, cancel blocked work, prove permit recovery and successful subsequent publication; held admission times out without network writes or local cleanup. fmt/check/all-feature Clippy/default tests pass (109 passed, 11 optional tests ignored). yhm01 1b095ab full export remains active, latest observed 7,321/13,367 resources, zero failures, 188.27 GiB free. It does not cover subsequent shared-budget changes. Remaining shared download/memory controls, other restoration, fresh-download and final-candidate release gates remain pending.

- Added service-wide max_downloads (default 4, range 1..64) for catalog/resource CDN attempts across all jobs while retaining each asset batch's own concurrency. Admission, response headers/body, file writes and sync share download_timeout_ms; cancelled/timed-out waiters release permits and do not contact the CDN before admission. Retries release slots before backoff; control requests and verified cache hits remain outside CDN admission. A real HTTP fixture with two independent clients proves one aggregate active download, cancellation handoff, subsequent success and queued timeout without a request/file. Existing resource download/retry tests pass under the common wrapper. fmt/check/all-feature Clippy/default tests pass (110 passed, 11 optional tests ignored). yhm01 earlier 1b095ab candidate still running: 8,201/13,367, zero failures, 158.58 GiB free; later shared budgets are not covered by that run. Shared memory/other original resource tuning, remaining registry/notification/persistence and final fresh-download/candidate/release requirements remain open.

- Restored optional max_in_flight_bundle_bytes in export and service configuration (0 disables), using weighted cancellable admission before decoding. Weight includes main-resource verified size and distinct on-disk dependency sizes; budgets remain held through output creation and release on failure/unwind/cancellation. Oversized resources consume the entire budget exclusively; cache hits bypass decode permits. This is a soft staged-source estimate, explicitly not a decoded-RSS ceiling; existing download/media/upload admission and OS limits remain separate. Tests cover weighted sharing, oversized exclusivity, blocked cancellation and real synthetic CRI decode recovery. Disabled budgets avoid extra dependency stat work. fmt/check/all-feature Clippy/default tests pass (112 passed, 11 optional tests ignored). yhm01 earlier candidate active at 11,679/13,367, zero failures, 141.74 GiB free; current budget changes require later candidate acceptance. CPU/stage tuning, remaining registry/notifications/persistence, full current-source download and final production/release gates remain open.

- Added real retained-export verification counters to job service progress. Verifier emits only hash-verified payload file/byte counts every 256 files and at resource boundaries; service persists latest values at most once per second with expected file total. Final inventory/exact-tree/journal checks still gate completion, and failure/cancellation never publishes success. Test confirms progress matches successful verification and corrupted payloads do not increment counters. fmt/check/all-feature Clippy/default suite pass (113 passed, 11 optional tests ignored). This addresses the production observation where 1b095ab remains at verify_export/0 until completion: that exact earlier task is still active, and /proc I/O samples increased rchar from 125,444,300,050 to 138,762,754,811 bytes, confirming continued work; no restart. Its verification/independent audit/incremental acceptance is not yet complete. Remaining restoration and final production/release requirements stay open.

- Removed the unnecessary four-resource worker ceiling: export concurrency now accepts 1..64 while retaining default 4. Existing worker scheduling uses the configured width; media and service-wide budgets remain independent. Documented explicit sizing, soft-byte/OS memory controls and multiplication across regional jobs. fmt/check/all-feature Clippy/default tests pass (113 passed, 11 optional tests ignored). This is operator-selected resource parallelism, not CPU auto-tuning or independent stage controls; high-width production acceptance remains pending. Active yhm01 1b095ab run remains unchanged and cannot validate this later change.

- Restored opt-in per-export CPU auto sizing: cpu.auto_tune/budget_auto/budget_ratio/reserved feed the actual resource worker pool. Default manual concurrency remains unchanged; automatic mode uses configured concurrency as a floor, CPU budget as the other floor, and twice available CPUs/64/resources as caps. Logs effective width; fallback CPU discovery is one. Generic Haruki post-process sizing only, without importing Sekai stage benchmark ratios. Invalid ratios fail even in manual mode. fmt/check/all-feature Clippy/default tests pass (115 passed, 11 optional tests ignored); actual FFmpeg service export/cache/restart/storage test explicitly passes while toggling CPU policy between runs. CPU usage throttling, independent stages and final high-width production acceptance remain pending.

- Audited original periodic download-record flushing against Sirius per-resource ciphertext cache commits. Existing verified entries already survive later task failure; no end-of-job-only progress dependency. Fixed forced-exit staging leakage: download startup acquires exclusive ownership before removing only generated download temporary names (including legacy six-character names), preserving decoded-export staging and unrelated entries. New cache commits sync staged and parent directory metadata on Unix in addition to existing file syncs; other platforms retain file syncs without a directory-durability claim. A real child process is killed after one commit and a second pending write: lock recovery, committed reuse, pending miss, legacy cleanup and external symlink-target preservation pass. fmt/check/all-feature Clippy/default suite pass (116 passed, 11 optional tests ignored). Per-resource sync is the current policy; configurable batching/performance comparison, final production restart acceptance and other restoration/release gates remain open.

- Restored independent per-export stage_limits for ACB containers, USM containers, native HCA decoding and Texture2D/Sprite decode+encode. Optional 1..64 caps default to no extra restriction; wait_timeout_seconds bounds admission, not synchronous execution. Nested acquisition is container then HCA/media, with HCA released before media validation/encoding. Cancelled/expired waits launch no stage work and permits release on all returns/unwind. Real encrypted ACB tests hold ACB/HCA gates, cancel blocked decoding before output, then verify PCM success after release; independent image/ACB admission and timeout recovery are covered. fmt/check/all-feature Clippy/default suite pass (118 passed, 11 optional tests ignored); real FFmpeg service/cache/restart/storage integration explicitly passes with all stage caps enabled. Waiting consumes a resource worker; fairness, CPU throttle, separate audio/video encoder widths, and final multi-stage corpus acceptance remain pending.

- Restored independent audio_encode/video_encode stage widths (optional 1..64) for FLAC/MP3 and MP4 conversions across CLI/FFI/Auto. Encoding admission precedes local/shared media admission and shares the original media deadline; Auto retains its encoding slot through fallback without resetting the clock. General remux/ADX/verification remains on the aggregate media gate. A real executable fixture proves blocked encoding cannot spawn, times out within the media budget, cancels, recovers and does not block the other encoding class. Corrected fixture argument handling for FFmpeg preflags and separated one-second admission checks from five-second child startup allowance. fmt/check/all-feature Clippy/default tests pass (119 passed, 11 optional ignored); real FFI service/cache/restart/storage and video/source-preservation/independent-decode tests explicitly pass with stage limits enabled. CPU throttle/stage auto-sizing and final production/corpus/release gates remain pending.

- Restored aggregate CPU-stage admission: opt-in profile cpu.limit_stages uses the CPU budget independently of worker auto_tune, and optional service max_cpu_stages (1..256) shares one pool across all export jobs. Unity loading/object conversion, CRI extraction, HCA and CLI/FFI execution acquire local then shared slots with cancellation and bounded waits; container/object stages release before nested audio work and HCA releases before media checks. CLI/FFI CPU admission shares original media deadlines, including Auto fallback. A one-slot two-configuration regression proves blocked native cancellation, media timeout before executable launch, local-slot release and subsequent encrypted HCA success; real FFI service/cache/restart/storage passes with a one-slot service pool. fmt/check/all-feature Clippy/default tests pass (120 passed, 11 optional ignored). CPU sampling/throttle, per-stage auto policy, final corpus/resource acceptance and all remaining platform/release gates stay open.

- Restored optional cpu.throttle.enabled/sample_ms (default off/250 ms; 50..60000) as actual process-tree feedback before instrumented CPU work. Linux samples per-PID/start-time CPU deltas so disappearing descendants cannot subtract surviving CPU; Unix fallback uses bounded/reaped ps with only numeric columns. Sampling is serialized/cached and lock/sleep waits observe cancellation and original deadlines; failures are explicit. Static CPU pools remain independently enabled. Windows rejects enabled throttle rather than silently returning zero. libc is now unconditional for Linux clock tick discovery; locked dependency versions unchanged. Tests cover process trees, exited/reused PIDs, high-to-low recovery, timeout, mid-wait cancellation and real macOS sampling. fmt/check/all-feature Clippy/default tests pass (123 passed, 11 ignored); actual FFI service/cache/restart/storage passes with throttle enabled. Linux host load-response, unsupported-platform policy and final corpus/resource/release gates remain pending.

- Restored opt-in stage_limits.auto_tune for actual ACB/USM/HCA/image/audio/video admission. Omitted caps derive from the profile CPU budget (1..64), with MP4 at half the budget based on the two encoder threads in both current CLI and FFI; explicit caps remain upper bounds. Worker and stage sizing now use the same CPU count captured on profile load. This does not claim benchmark-optimal throughput, increase media/service limits or replace aggregate throttling. Effective widths are logged and remain outside cache identity. Tests cover manual/automatic policies, narrow/huge hosts and explicit caps, actual encrypted ACB/HCA cancellation/recovery and encoder timeout-before-spawn with computed limits. fmt/check/all-feature Clippy/default tests pass (124 passed, 11 optional ignored); both actual FFmpeg CLI and FFI service export/restart/cache/storage tests explicitly pass while toggling stage auto sizing. Sustained Linux load response, final high-width corpus/resource acceptance and remaining restoration/release gates stay open.

- Added and executed the opt-in Linux real-child CPU feedback test on yhm01 at source 5d75936392a463519ac5a1b5e2a25a9f7d579609, under an isolated 2-CPU/8-GiB/600-second unit. The test sampled a 205.0% peak, enforced the 700 ms one-core admission deadline, handled cancellation, and recovered 265 ms after child termination; all five CPU tests and both stage-limit tests passed. Unit exited successfully with no live PID. Existing Haruki updater start times and the prior baseline standalone binary SHA-256 stayed unchanged. Local fmt/check/all-feature Clippy/default tests pass (124 passed, 11 optional ignored); Linux-only fixture/test are deliberately opt-in. This verifies short real process-tree load response, not long-running multi-job/full-corpus performance. Remaining restoration and final fresh-download/full-candidate/platform/release gates remain open.

- Restored S3 requester-pays and multipart-compatible CRC32C upload policy as typed backend.request_payer and backend.write_options.checksum_algorithm. Defaults preserve existing requests. Real local signed S3 publication covers multipart payloads, receipt/marker writes and authenticated readback; an independent CRC32C implementation with the standard check vector validates transmitted body checksums. Requester headers reach all operations, while denied verification preserves local exports and never retries by removing policy. MD5 is rejected during validation because the locked OpenDAL backend does not support it for multipart initiation; other unsupported algorithms fail explicitly. Updated migration docs and storage example. Required fmt/check/all-feature Clippy/default tests pass127/11optionalignored. Deployed-store acceptance, remaining scalar/template migration, completion notifications/registry and final full-candidate resource/platform/release gates remain pending.

- Restored generic region-template routing adapted to Sirius: {region} and migration alias {server} resolve only to jp/tw/en/kr in provider prefix/public URL, local directory and S3 bucket/endpoint. Plan and publish share resolved providers and existing validation; immutable region/publication suffixes remain mandatory, credentials and S3 signing-region are not interpolated, and CN fails before I/O. Actual four-region synthetic local/S3 publication verifies directory/bucket placement, public URLs and plan consistency; tests reject unknown tokens, unsafe URLs, credential templates and resolved source overlap. Required fmt/check/all-feature Clippy/default tests pass130/11optionalignored. Deployed storage, remaining scalar-option audit, notifications/registry, final resource/platform/full-candidate production/release gates remain pending.

- Restored S3 customer-provided AES256 encryption via write_options.customer_key_base64_env. Strict standard Base64/32-byte validation and mutual exclusion with SSE-S3/KMS happen before publishing; OpenDAL derives key MD5 and attaches encryption headers to writes, multipart operations and verification reads. Real synthetic-key fixture verifies all request headers against independent key/MD5 vectors, including multipart and markers, and checks receipts do not expose key/env references. Required checks pass132/default11optionalignored. Added an explicit S3 scalar-option migration/applicability matrix: internal profile/STS acquisition/refresh and additional ACL modes remain gaps; version-list/delete/append knobs do not correspond to current immutable publication operations. This audit does not complete broad storage or production parity. Notifications/registry, remaining platform/resource and exact final candidate/release gates stay open.
