# Preview: sirius-asset-updater plan-storage publish-config.yaml
# Run: sirius-asset-updater publish publish-config.yaml
input: ./exports/retained-export
region: jp
storage:
  providers:
    - name: local
      prefix: assets
      backend:
        type: local
        directory: ./published-assets
  concurrency: 4
  attempts: 3
  object_timeout_seconds: 300
  retry_delay_ms: 500
  remove_local_after_upload: false

# Optional application events; default is info/text on stderr. See docs/APPLICATION_LOG.md.
# logging:
#   level: info
#   format: json
#   output: {type: file, path: ./logs/application.log, rotation: daily, max_files: 7}
#   queue_capacity: 4096
